
    <div class="row">
        <div class="col-md-5">
            <div class="form-group">
                <h3>Создать новый</h3>
                <form action="/pages/create" method="POST">
                <input type="text" class="form-control" required name="name">
                <button class="btn btn-outline-success" type="submit">Ок, создадим</button>
                </form>
            </div>
        </div>
    </div>
</div>

<div class="container-fluid">
    <div class="row">
            {{#if pages}}
                {{#each pages}}
             <div class="col-md-2 question-card"> 
                    <a href="/pages/{{this.id}}">{{this.name}}</a>
            </div>
                {{/each}}
            {{/if}}
    </div>